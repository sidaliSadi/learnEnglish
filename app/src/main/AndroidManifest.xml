<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projet">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Listes"
            android:label="Mes Listes"
            >

        </activity>
        <activity android:name=".PratiquerActivity"
            android:label="Testez vous connaissance"
            />
        <activity android:name=".VocabularyActivity"
            android:label="Vocabulaire"
            />
        <activity
            android:name=".ApprendreActivity"
            android:label="Apprentissage"
            />
        <activity android:name=".GestionBD"
            android:label="Ajoutez une Traduction"
            >
        </activity>
        <activity android:name=".Traduction"
            android:label="Traduction de votre liste"
            ></activity>
        <activity android:name=".MainActivity"
                android:label="Acceuil"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>